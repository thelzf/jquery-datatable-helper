# jquery-datatable-helper

A simple helper to initialize jQuery DataTables with export buttons (Excel, PDF, Print, Word) and custom configuration.

## Installation

```bash
npm install jquery-datatable-helper
